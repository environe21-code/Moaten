# 게임 개발 공통 노하우 (Chae Lab)

> 용도: 새 게임 개발 채팅의 첫 메시지에 첨부. 여기 적힌 사항은 개발 초기 단계부터 기본값으로 반영할 것.
> 출처: 모아텐(MoaTEN)·블록체이스(BLOCK CHASE) 개발 및 모아텐 Play 스토어 출시 파이프라인 경험.
> 원칙: 모아텐이 먼저 밟은 시행착오를 다음 게임은 건너뛴다.

## 0. 개발 체제 (역할 분담)
- 개발자: Chae Lab (비개발자 크리에이티브 디렉터) — 방향 결정, 디자인 판단, QA, 실기기 테스트, GitHub/콘솔 업로드
- 코드는 전부 Claude가 작성. 설명은 비개발자 기준으로 (전문용어는 풀어서)

## 1. 소스 관리 — 가장 중요한 교훈
- **소스 프로젝트를 개발 채팅 안에만 두지 말 것.** 채팅 환경은 세션마다 리셋되고, 배포된 압축 빌드본에서 소스 역추출은 불가능하다.
- 소스는 **GitHub 저장소에 보관** (게임 배포용 저장소와 별도). 이렇게 하면 어느 채팅에서든 Claude가 소스를 내려받아 제대로 수정·빌드 가능.
- 배포본(빌드된 index.html)에 직접 가하는 패치는 **응급처치 전용**. 반드시 "소스 반영 대기 목록"에 기록하고 다음 소스 작업 때 소스에도 반영할 것 (안 하면 다음 빌드에서 패치가 사라져 증상이 재발한다).
- 채팅 전환 시에는 인수인계 문서(.md)를 만들어 새 채팅 첫 메시지에 첨부하는 방식 유지.

## 2. 게임 설계 기본값 (앱 출시를 전제로 처음부터 넣을 것)
- **실행 시 항상 초기화면(타이틀) 우선.** 진행 중이던 판이 있으면 자동으로 게임 화면에 직행하지 말고, 해당 모드 선택 시 "진행 중인 게임이 있어요 → 이어하기 / 새로 시작" 모달로 처리.
- **뒤로가기 버튼 처리 (안드로이드):**
  - 게임 중 뒤로가기 → "계속하기 / 처음 화면으로" 선택창 (처음 화면으로 = 타이틀 복귀)
  - 초기화면에서 뒤로가기 → 종료 확인 후 더블백(한 번 더 누르면 종료)
  - ⚠️ 플랫폼 규칙: TWA에서는 웹 코드가 앱을 직접 종료할 수 없다. 최종 종료는 항상 시스템 뒤로가기가 수행 → "버튼 즉시 종료"는 설계하지 말 것.
  - ⚠️ Chrome 기록조작 방지: 사용자의 첫 터치 이전에 쌓은 히스토리 항목은 뒤로가기 때 무시된다 → 뒤로가기 가드는 첫 제스처 이후에 활성화.
- **진행 상태 자동 저장** (localStorage) — 도중에 꺼져도 판 유지. 판이 정상 종료되면 저장분 삭제.
- **다국어 구조를 처음부터** (모아텐 기준 7개 언어: ko, en, es, pt, id, ja, zh-TW). 스크린샷 캡처용 언어 강제 스위치(window.__LANG 방식)도 함께.
- **온라인 랭킹 대비 구조 선반영**: 별명 입력 UI·명예의전당·데이터 구조는 게임에 미리 구현해 두고, 서버(Firebase) 연결만 출시 후 첫 업데이트로. 연결 시 부정 방지 + 별명 필터 + Play Console 데이터 보안 양식 갱신(필수)이 한 세트.

## 3. PWA/TWA 패키징 (PWABuilder)
- **manifest.json `display: "fullscreen"`** — 상단 상태바·하단 내비게이션 바 숨김. standalone이면 시스템 UI가 보인다.
- ⚠️ display 설정과 앱 아이콘은 **AAB 빌드 시점에 앱에 구워진다** → 웹 배포로는 못 바꾸고 재빌드 필요. 아이콘(icon-192/512/maskable)은 빌드 전에 저장소에서 확정할 것. 스토어 등록정보의 아이콘과는 별개다.
- 배포 index.html **필수 주입 3종**: `<link rel="manifest">` + sw.js 등록 스크립트 + 애드센스 스크립트. 새로 빌드해 배포할 때마다 재주입 확인.
- sw.js는 **네트워크 우선** 전략 (웹 배포만으로 게임 업데이트가 즉시 반영되는 근거).
- PWABuilder 재빌드 체크: 동일 Package ID / version code 증가 / Signing "Use mine"(기존 keystore+비밀번호) / Notification delegation 해제.
- **서명키(keystore)+비밀번호는 클라우드에 백업.** 분실 시 앱 업데이트 자체가 불가능해진다.
- 배포 저장소 기본 구성: index.html, manifest.json, sw.js, icon 3종, privacy.html, .well-known/assetlinks.json
- GitHub Pages는 커밋 후 **CDN 캐시 최대 ~10분** — 반영 확인은 폰 Chrome에서 먼저, 그다음 앱(완전 종료 후 재실행).

## 4. Play Console / 출시 파이프라인
- **프로덕션 액세스 심사 기준 2가지를 테스트 기간 중에 챙길 것** (모아텐 1차 반려 사유):
  1) 테스터 실사용 집계 (설치만으론 부족, 실행·유지 필요. TWA는 실행 집계가 덜 잡히는 경향 → 테스터에게 실행 루틴 명확히 요청)
  2) **콘솔 AAB 업데이트 이력** — 개선을 웹 배포로만 하면 콘솔 기록상 업데이트 0회가 된다. 테스트 기간 중 의도적으로 AAB를 1~2회 업로드할 것.
- 릴리스는 업로드만 하면 "임시(초안)" 상태 — **"검토를 위해 전송"까지 눌러야** 검토가 시작된다. 스토어 등록정보 수정도 동일 (저장 ≠ 게시).
- 비공개 트랙에서 국가를 수동 추가하면 프로덕션과의 동기화가 자동 해제된다 — 정상 동작이니 놀라지 말 것.
- 스토어 등록정보:
  - 오른쪽 **애셋 라이브러리는 업로드 보관함일 뿐** — 스토어 노출은 각 리스팅의 "슬롯"에 꽂힌 이미지만. 라이브러리의 이미지는 어떤 슬롯에서든 사용 중이면 삭제 불가.
  - 그래픽 슬롯은 휴대전화 외에 **7인치/10인치 태블릿, Chromebook이 별도** — 데스크톱 웹 스토어는 태블릿 슬롯을 우선 표시. 구버전 정리 시 전 슬롯 확인.
  - 휴대전화 스크린샷 최소 2장 유지.
  - 언어별 리스팅에 그래픽 미등록 시 기본 리스팅 것이 자동 공유 (의무 아님). 언어별 스샷은 출시 후 유입 국가 확인 뒤 필요한 언어만 제작.
  - 스토어 웹페이지는 검토 통과 후에도 캐시로 몇 시간 시차 가능. 언어 확인은 URL에 `&hl=ko` 등 부여.
- 관리형 게시 "사용 중지됨" = 검토 통과 시 자동 게시 (정상).

## 5. 수익화 (애드센스)
- 애드센스 심사에는 게임 링크만으론 부족 — **콘텐츠가 있는 랜딩/소개 페이지**(루트 저장소)가 필요.
- ⚠️ 랜딩 페이지는 루트 저장소에만. **게임 저장소의 index.html을 덮어쓰지 않도록 주의** (모아텐에서 실제 발생한 사고 — 게임이 소개 페이지로 바뀌어 버림. 복구는 GitHub 커밋 이력에서 가능).
- 광고 위치 기본값: 시작 화면 하단 / 종료(결과) 화면. 웹 배포만으로 반영 가능.
- 수익 기대치: 다운로드 수천 건 전까지는 미미 — 초기엔 수익보다 심사 통과·구조 마련에 의미.

## 6. 테스트·배포 문제해결 순서 (증상별)
- 웹 수정이 앱에 반영 안 됨 → ① GitHub 커밋 확인 ② CDN 캐시 ~10분 대기 ③ 폰 Chrome으로 직접 접속해 확인 ④ 앱 완전 종료 후 재실행 ⑤ 최후: 앱 데이터 삭제
- 앱이 이상한 페이지를 보여줌 → 게임 저장소 index.html이 다른 파일로 덮였는지 확인 (커밋 이력 대조)
- 앱 아이콘/전체화면이 안 바뀜 → AAB에 구워지는 항목 — 재빌드 필요 (웹 배포로는 불가)
